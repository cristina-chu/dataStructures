/*
public class SomeBT {

	public String inOrder(BTNode here) {
		String res = "";
		if (here != null) {
			String lstr = inOrder(here.left);
			String rstr = inOrder(here.right);
			res = lstr + here + rstr;
			// LCR is what makes the inOrder traversal 
		}
		return res;
	}
	
	public String preOrder(BTNode here) {
		String res = "";
		if (here != null) {
			String lstr = preOrder(here.left);
			String rstr = preOrder(here.right);
			res = here + lstr + rstr;
			// CLR is what makes the preOrder traversal 
		}
		return res;
	}
	
}*/
